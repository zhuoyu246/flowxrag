{
  "title": "Kafka",
  "schemaVersion": 39,
  "panels": [
    {"type":"timeseries","title":"Consumer lag","targets":[{"expr":"kafka_consumer_lag"}]},
    {"type":"timeseries","title":"Messages in / DLQ","targets":[{"expr":"sum(rate(kafka_messages_total[5m])) by (outcome)"}]},
    {"type":"timeseries","title":"Processing time P95","targets":[{"expr":"histogram_quantile(0.95, sum(rate(kafka_message_processing_seconds_bucket[5m])) by (le))"}]}
  ]
}
